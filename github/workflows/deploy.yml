script: |
  echo "Starting deploy: $(date)"
  cd /var/testcicd || exit 1
  git fetch origin
  git reset --hard origin/main
  git clean -fd
  npm install
  echo "Restarting PM2: $(date)"
  pm2 restart server
  echo "Deploy complete: $(date)"
